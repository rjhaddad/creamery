$('#new_shift').remove();
$('#new_shift_link').show();
$('#list_shifts').replaceWith('<%= j render partial: 'list_shifts', locals: {store: @store, shifts: @shifts} %>');
$('#shift_future').replaceWith('<%= j render partial: 'assignments/shift_future', locals: {store: @store, shifts: @upcoming_shifts} %>');
$('#list_new_shifts').replaceWith('<%= j render partial: 'home/list_new_shifts', locals: {store: @store, shifts: @new_shifts} %>');
