<p>&nbsp;</p>

<div class="card" style="width: 22rem;">
	<center><h3>Today's Shifts</h3></center>
<center><h6 class="card-subtitle mb-2 text-muted"><%= Date.current.strftime("%A %B %d, %Y") %></h6>

  <div class="card-body">
		<% unless @today_shifts.nil? or @today_shifts.empty? %>
			<table class="table table-striped">
			    <thead>
			      <tr>
			        <th>Date</th>
			        <th>Start Time</th>
			        <th>End Time</th>
			        <th>&nbsp;</th>
			      </tr>
			    </thead>
			    <tbody>
			      <% @today_shifts.each do |shift| %>
			        <tr>
			          <td><%= shift.date.strftime("%m/%d/%y") %></td>
			          <td><%= shift.start_time.localtime.strftime("%I:%M %p") %></td>
			          <td><%= shift.end_time.localtime.strftime("%I:%M %p") %></td>
			          <% if Time.now.strftime("%H:%M %p") > shift.end_time.strftime("%H:%M %p") %>
				          <td>
				          	<div id = "view-jobs-button">
				            <%= link_to(shift_path(shift), class: 'button tiny radius warning') do %>
				              &nbsp;View Jobs&nbsp;
				            <% end %>
				        	</div>
		          		  </td>
	          		  <% else %>
			        	 	<td>&nbsp;</td>
	          		  <% end %>

			        </tr>
			      <% end %>
			    </tbody>
		  </table>
 
		  
		  

	  <% else %>
	  <p>You have no shifts today.</p>
	  <% end %>

	 </div>
	 </center>
	 </div>


<div class="card" style="width: 20rem;">
	<center>	<h3>Past Shifts</h3> </center>

	<div class="card-body">
				<% unless @past_shifts.nil? or @past_shifts.empty? %>

		<table class="table table-striped">
		    <thead>
		      <tr>
		        <th>Date</th>
		        <th>Start Time</th>
		        <th>End Time</th>
		        <th>&nbsp;</th>

		      </tr>
		    </thead>
		    <tbody>
		      <% @past_shifts.each do |shift| %>
		        <tr>
		          <td><%= shift.date.strftime("%m/%d/%y") %></td>
		          <td><%= shift.start_time.localtime.strftime("%I:%M %p") %></td>
		          <td><%= shift.end_time.localtime.strftime("%I:%M %p") %></td>
		          <% if shift.completed? %>
			          <td>
			          	<div id = "view-jobs-button">
			            <%= link_to(shift_path(shift), class: 'button tiny radius warning') do %>
			              &nbsp;View Jobs&nbsp;
			            <% end %>
			        	</div>
	          		  </td>
	          	  <% else %>
	          	  	<td>&nbsp;</td>
          		  <% end %>


		        </tr>
		      <% end %>
		      

		    </tbody>
	  </table>
  <% else %>
	 <center> <p>You have no past shifts.</p></center>
	  <% end %>

	 </div>
</div>


<div class="card" style="width: 20rem;">
<center>	<h3>Upcoming Shifts</h3>

	 <div class="card-body">
	  <% unless @upcoming_shifts.empty? %>
		<table class="table table-striped">
		    <thead>
		      <tr>
		        <th>Date</th>
		        <th>Start Time</th>
		        <th>End Time</th>
		      </tr>
		    </thead>
		    <tbody>
		      <% @upcoming_shifts.each do |shift| %>
		        <tr>
		          <td><%= shift.date.strftime("%m/%d/%y") %></td>
		          <td><%= shift.start_time.localtime.strftime("%I:%M %p") %></td>
		          <td><%= shift.end_time.localtime.strftime("%I:%M %p") %></td>
		        </tr>
		      <% end %>
		    </tbody>
	  	</table>

	  <%else%>
	  	<p>You have no upcoming shifts.</p>
	  <% end %>

</div>
</center>
</div>



<div class="card" style="width: 20rem;">
<center>	<h3> Shift Jobs</h3>

	 <div class="card-body">

</div>

</div>