<div class= "inner2">
<div class="image-holder">
	<%= link_to image_tag('user.png', class:"img2") %>
</div>
				

	<%= simple_form_for @user, html: { class: 'form-horizontal' }, remote: true do |f| %>
	
	 <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	  <fieldset>

 <legend><%= controller.action_name.capitalize %> User</legend>
 
  <div class="form-wrapper" >		
  
        <%= f.input :employee_id, :collection => Employee.active.alphabetical, :class => "form-control" %>
</div>

  <div class="form-wrapper">
        <%= f.input :email, :label => "Email Address ", :class => "form-control" %>
</div>
     
  <div class="form-wrapper">
        <%= f.input :password, :label => "Password", :class => "form-control" %>
</div>
        
  <div class="form-wrapper">
        <%= f.input :password_confirmation, :label => "Password Confirmation", :class => "form-control" %>
</div>
        
        
        <div class="form-actions">
          <%= f.submit nil, :class => 'btn btn-dark' %>
          <%= link_to 'Cancel',users_path, :class => 'btn' %>
        </div>
        
</fieldset>


<% end %>
</div>